🛠 Обновление проекта (CI/CD Manual)
Чтобы обновить проект на сервере до последних версий:

Подтяните изменения инфраструктуры и указатели сабмодулей:

Bash
git pull origin main
Обновите код внутри сабмодулей:

Bash
git submodule update --remote --recursive
Пересоберите контейнеры:

Bash
docker-compose up --build -d